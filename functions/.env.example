# Environment variables for Firebase Functions (dotenv)
# These are loaded automatically by the Firebase CLI from .env* files in the functions/ directory.
# Do NOT commit real secrets. Keep .env and .env.* out of git.

VAPID_PUBLIC_KEY=your_public_key_base64url
VAPID_PRIVATE_KEY=your_private_key_base64url
VAPID_CONTACT=mailto:you@example.com
